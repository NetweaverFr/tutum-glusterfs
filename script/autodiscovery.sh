#bin/sh

# List all the container from the gluster service

    # if container number == 0 or 1
        # show a message
    # else if container number > 1
        # For each available container
            # if the container is not in the glusterfs cluster
                # Peer glusterfs container
                #
            # else
                # do nothing

# wait 60 seconds
